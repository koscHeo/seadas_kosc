#include "l12_proto.h"

int openl1_viirs_nc(filehandle * file) {
    printf("Error - VIIRS L1A reader is not implemented.\n");
    exit(EXIT_FAILURE);

    return (LIFE_IS_GOOD);
}

int readl1_viirs_nc(filehandle *file, int32 line, l1str *l1rec)
{
    printf("Error - No Really, the VIIRS L1A reader is not implemented.\n");
    exit(EXIT_FAILURE);

    return (LIFE_IS_GOOD);
}

int readl1_lonlat_viirs_nc(filehandle *file, int32 line, l1str *l1rec)
{
    printf("Error - No Really, the VIIRS L1A reader still is not implemented.\n");
    exit(EXIT_FAILURE);

    return (LIFE_IS_GOOD);
}

int closel1_viirs_nc(filehandle *file) {
    printf("Error - the VIIRS L1A reader is not implemented.\n");
    exit(EXIT_FAILURE);

    return (LIFE_IS_GOOD);
}

